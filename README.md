# 🔢 Insertion Sort Algorithm in C

A simple C program that sorts an array in **ascending order using the Insertion Sort algorithm**.

### Concepts Used

* Arrays
* Functions
* Loops
* `while` loop
* Sorting algorithms

The program builds the sorted array one element at a time by inserting each element into its correct position.
